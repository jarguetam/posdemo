import { FocusLastInputDirective } from './focus-last-input.directive';

describe('FocusLastInputDirective', () => {
  it('should create an instance', () => {
    const directive = new FocusLastInputDirective();
    expect(directive).toBeTruthy();
  });
});
